<?xml version="1.0" encoding="utf-8"?>

<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <layer-list>
            <!-- CONTENT LAYER -->
            <item>
                <shape android:shape="oval">
                    <!-- transparent stroke = larger_circle_size - smaller_circle_size -->
                    <stroke android:width="4dp" android:color="@color/alignment_canvasOutline" />
                    <solid android:color="@color/alignment_canvasBackground" />

                </shape>
            </item>
        </layer-list>
    </item>
</selector>